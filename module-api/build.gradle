bootJar {
    enabled = true
}

jar {
    enabled = false
}

dependencies {
    implementation project(":domain")
    implementation project(":support:logging")
    runtimeOnly project(":storage:db-module")

    implementation 'org.springframework.boot:spring-boot-starter-web'
}